provider:
  name: "aws"
plugins:
  # - "@fauna-labs/serverless-fauna"
  - "."
service: "foo"
fauna:
  client:
    secret: secret
    domain: localhost
    port: 8443
    scheme: http
  collections:
    movies:
      name: movies
  indexes:
    movies_ts:
      name: movies_ts
      source: movies
